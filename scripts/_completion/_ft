#compdef ft

emulate -L zsh -o extendedglob

# Delegate to the `fzf-tools` completion when provided by the native installation.
words[1]='fzf-tools'

autoload -Uz _fzf-tools 2>/dev/null || true
if (( $+functions[_fzf-tools] )); then
  _fzf-tools
  return $?
fi

_message 'fzf-tools'
return 0

