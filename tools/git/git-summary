#!/usr/bin/env -S zsh -f

setopt no_nomatch

source "$ZDOTDIR/scripts/git/git-summary.zsh"

if ! typeset -f git-summary >/dev/null; then
  printf "❗ 'git-summary' is not defined as a function.\n"
  exit 1
fi

git-summary "$@" || exit $?
