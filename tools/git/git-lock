#!/usr/bin/env zsh

setopt no_nomatch

source "$ZDOTDIR/scripts/git/git-lock.sh"

if ! typeset -f git-lock >/dev/null; then
  echo "❗ 'git-lock' is not defined as a function."
  exit 1
fi

git-lock "$@" || exit $?