#!/usr/bin/env -S zsh -f

setopt no_nomatch

source "$ZDOTDIR/scripts/git/git-scope.zsh"

if ! typeset -f git-scope >/dev/null; then
  printf "❗ 'git-scope' is not defined as a function.\n"
  exit 1
fi

git-scope "$@" || exit $?
